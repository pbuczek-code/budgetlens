[project]
name = "budgetlens"
version = "0.1.0"
description = "Interactive personal finance dashboard built with Python and Streamlit."
readme = "README.md"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]